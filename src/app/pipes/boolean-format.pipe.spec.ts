import { BooleanFormatPipe } from './boolean-format.pipe';

describe('BooleanFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
