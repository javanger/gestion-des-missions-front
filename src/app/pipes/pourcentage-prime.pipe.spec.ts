import { PourcentagePrimePipe } from './pourcentage-prime.pipe';

describe('PourcentagePrimePipe', () => {
  it('create an instance', () => {
    const pipe = new PourcentagePrimePipe();
    expect(pipe).toBeTruthy();
  });
});
